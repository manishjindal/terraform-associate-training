```
terraform console
```

```
max(12, 54, 3)
split(",", "foo,bar,baz")
trimprefix("helloworld", "hello"
zipmap(["a", "b"], [1, 2])
length(["a", "b"])
base64decode("SGVsbG8gV29ybGQ=")
base64encode("Hello World")
```

lookup
element
zipmap
max
